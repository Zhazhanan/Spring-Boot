<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//com.order.test.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.transaction.dao.UserDao">
    <resultMap id="userMap" type="com.example.transaction.dto.User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
        <result property="createTime" column="createTime" jdbcType="DATE" />
    </resultMap>

    <insert id="save" parameterType="Object" useGeneratedKeys="true">
        INSERT INTO codetest.user (id, name, password, createTime)
        VALUES (#{id}, #{name}, #{password}, #{createTime});
    </insert>

    <update id="updateUser">
        UPDATE codetest.user SET
        `name` = #{name},
        password = #{password},
        createTime = #{createTime}
        WHERE id = #{id};
    </update>
</mapper>